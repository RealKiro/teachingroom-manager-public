# Docker Compose 配置模板
# 用法：把本文件复制为 .env 并修改（docker compose 会自动读取同目录的 .env）

# 必改：站点会话密钥，任意长随机字符串（可用 openssl rand -hex 48 生成，或用密码生成器）
SESSION_SECRET=change-me-to-a-long-random-string

# 可选：首次启动的管理员密码（至少 12 位；不设置则打印在首次启动日志中）
#INITIAL_ADMIN_PASSWORD=

# 可选：自定义镜像（默认拉取官方预构建镜像，Fork 用户可指向自己账号下的 GHCR 包）
#TEACHINGROOM_IMAGE=ghcr.io/realkiro/teachingroom-manager-public:latest
